# Setup the following aliases in your ~/.bashrc.
#
# In order to have everything from the ecosystem readily available, you'll just
# need to source the alias file. #  the following alias comes in handy:
# alias sl='cd $THIS_DIR && source ~/src/GSWL-ecosystem/alias' # Start Ledger
